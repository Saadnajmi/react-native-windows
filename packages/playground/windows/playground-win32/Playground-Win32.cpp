#include "pch.h"
#include "resource.h"

#include <windows.h>

int WinMain(HINSTANCE instance, HINSTANCE, PSTR commandLine, int showCmd) {
  UNREFERENCED_PARAMETER(instance);
  UNREFERENCED_PARAMETER(instance);
}
